import { styled, Typography } from "@mui/material";

export const CustomHeading = styled(Typography)({
    fontSize: '40px',
    textAlign: 'center',
    marginBottom: '40px',
    fontWeight : 800,
  });
